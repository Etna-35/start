[project]
name = "max-chrono-bot"
version = "0.1.0"
description = "MVP text bot for personal time tracking (MAX Bot API)"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-q"

[tool.ruff]
line-length = 100
target-version = "py312"
